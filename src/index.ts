/** Node half: the bundle row exists so DSH discovers the package's client manifest. */
export const name = 'accessibility'

/** The optional companion has no Host services or state. */
export function apply(): void {}
